#include <stdio.h>

int main() {
    int num;       // Two numbers.

    // Input
    printf("Enter number: ");
    scanf("%d", &num);

    // Chick and print.
    if(num > 0){
        printf("%d is Positive.",a);
    }
    else if(num < 0){
        printf("%d is Negative.",a);
    }
    else{
        printf("It's zero.");
    }
    

    return 0;
}
